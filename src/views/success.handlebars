<head>
        <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            margin: 0;
            padding: 0;
        }

        .container-success {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        .container-success h1 {
            color: #1e90ff;
        }

        .container-success h2 {
            color: #1e90ff;
            margin-top: 20px;
        }

        .container-success p {
            margin: 10px 0;
        }

        .container-success a {
            color: #1e90ff;
            text-decoration: none;
            display: block;
            margin-top: 20px;
        }

        .container-success a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<div class="container-success">
    <h1>¡Gracias por comprar en nuestro e-commerce!</h1>

    <div>
        <h2> Ticket: </h2>
        <div>
            <p>Código: {{ticket.code}}</p>
            <p>Total: {{ticket.amount}}</p> 
            <p>Fecha: {{ticket.purchase_datetime}}</p> 
        </div>
        <p>Puedes visualizar el ticket de la compra en el correo electrónico registrado</p>
    </div>

    <a href="/">Volver a la página principal</a>

 </div>